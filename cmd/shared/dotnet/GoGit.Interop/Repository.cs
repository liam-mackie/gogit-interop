// <auto-generated/>
#nullable enable
using System.Text.Json;

namespace GoGit.Interop;

public sealed class Repository : IDisposable
{
    private long _handle;
    private bool _disposed;

    internal Repository(long handle) => _handle = handle;

    public static Repository Init(string path, bool isBare = false)
    {
        NativeMethods.ThrowIfError(NativeMethods.GitPlainInit(path, isBare ? 1 : 0, out var handle));
        return new Repository(handle);
    }

    public static Repository Open(string path)
    {
        NativeMethods.ThrowIfError(NativeMethods.GitPlainOpen(path, out var handle));
        return new Repository(handle);
    }

    public static Repository OpenWithOptions(string path, PlainOpenOptions options)
    {
        NativeMethods.ThrowIfError(NativeMethods.GitPlainOpenWithOptions(path, options.Handle, out var handle));
        return new Repository(handle);
    }

    public static Repository Clone(string path, CloneOptions options)
    {
        NativeMethods.ThrowIfError(NativeMethods.GitPlainClone(path, options.Handle, out var handle));
        return new Repository(handle);
    }

    public (string RefName, string Hash) Head()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryHead(_handle, out var refName, out var hash));
        return (NativeMethods.ConsumeGoString(refName)!, NativeMethods.ConsumeGoString(hash)!);
    }

    public Worktree GetWorktree()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryWorktree(_handle, out var wtHandle));
        return new Worktree(wtHandle);
    }

    public void Fetch(FetchOptions options)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryFetch(_handle, options.Handle));
    }

    public void Push(PushOptions options)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryPush(_handle, options.Handle));
    }

    public CommitIterator Log(LogOptions? options = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        var optsHandle = options?.Handle ?? 0;
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryLog(_handle, optsHandle, out var iterHandle));
        return new CommitIterator(iterHandle);
    }

    public ReferenceIterator Tags()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryTags(_handle, out var iterHandle));
        return new ReferenceIterator(iterHandle);
    }

    public ReferenceIterator Branches()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryBranches(_handle, out var iterHandle));
        return new ReferenceIterator(iterHandle);
    }

    public ReferenceIterator Notes()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryNotes(_handle, out var iterHandle));
        return new ReferenceIterator(iterHandle);
    }

    public ReferenceIterator References()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryReferences(_handle, out var iterHandle));
        return new ReferenceIterator(iterHandle);
    }

    public (string RefName, string Hash) Reference(string name, bool resolved = true)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryReference(_handle, name, resolved ? 1 : 0, out var refName, out var hash));
        return (NativeMethods.ConsumeGoString(refName)!, NativeMethods.ConsumeGoString(hash)!);
    }

    public string ResolveRevision(string rev)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryResolveRevision(_handle, rev, out var hash));
        return NativeMethods.ConsumeGoString(hash)!;
    }

    public Remote CreateRemote(string name, string url)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryCreateRemote(_handle, name, url, out var handle));
        return new Remote(handle);
    }

    public Remote GetRemote(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryRemote(_handle, name, out var handle));
        return new Remote(handle);
    }

    public void DeleteRemote(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryDeleteRemote(_handle, name));
    }

    public string[] Remotes()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryRemotes(_handle, out var jsonPtr));
        var json = NativeMethods.ConsumeGoString(jsonPtr)!;
        return JsonSerializer.Deserialize<string[]>(json) ?? [];
    }

    public void CreateBranch(string name, string hash)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryCreateBranch(_handle, name, hash));
    }

    public void DeleteBranch(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryDeleteBranch(_handle, name));
    }

    public (string RefName, string Hash) CreateTag(string name, string hash, CreateTagOptions? options = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        var optsHandle = options?.Handle ?? 0;
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryCreateTag(_handle, name, hash, optsHandle, out var refName, out var hashOut));
        return (NativeMethods.ConsumeGoString(refName)!, NativeMethods.ConsumeGoString(hashOut)!);
    }

    public (string RefName, string Hash) Tag(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryTag(_handle, name, out var refName, out var hash));
        return (NativeMethods.ConsumeGoString(refName)!, NativeMethods.ConsumeGoString(hash)!);
    }

    public void DeleteTag(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryDeleteTag(_handle, name));
    }

    public Commit GetCommitObject(string hash)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryCommitObject(
            _handle, hash,
            out var commitHash, out var msg, out var authorName, out var authorEmail, out var ts));
        return new Commit
        {
            Hash = NativeMethods.ConsumeGoString(commitHash)!,
            Message = NativeMethods.ConsumeGoString(msg)!,
            AuthorName = NativeMethods.ConsumeGoString(authorName)!,
            AuthorEmail = NativeMethods.ConsumeGoString(authorEmail)!,
            AuthorTimestamp = DateTimeOffset.FromUnixTimeSeconds(ts),
        };
    }

    public void Merge(string refName, string hash, MergeOptions? options = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        var optsHandle = options?.Handle ?? 0;
        NativeMethods.ThrowIfError(NativeMethods.GitRepositoryMerge(_handle, refName, hash, optsHandle));
    }

    public void Dispose()
    {
        if (_disposed) return;
        _disposed = true;
        NativeMethods.GitRepositoryFree(_handle);
        _handle = 0;
    }
}
