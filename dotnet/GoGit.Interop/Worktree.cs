// Code generated by gogit-interop/generate. DO NOT EDIT.
#nullable enable
using System.Text.Json;

namespace GoGit.Interop;

public sealed class Worktree : IDisposable
{
    private long _handle;
    private bool _disposed;

    internal Worktree(long handle) => _handle = handle;
    internal long Handle => _handle;

    public string Add(string path)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeAdd(_handle, path, out var hash));
        return NativeMethods.ConsumeGoString(hash)!;
    }

    public void AddGlob(string pattern)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeAddGlob(_handle, pattern));
    }

    public void AddWithOptions(AddOptions? opts = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeAddWithOptions(_handle, opts?.Handle ?? 0));
    }

    public void Checkout(CheckoutOptions? opts = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeCheckout(_handle, opts?.Handle ?? 0));
    }

    public void CherryPick(CommitOptions commitOpts, int ortStrategyOption)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeCherryPick(_handle, commitOpts.Handle, ortStrategyOption));
    }

    public void Clean(CleanOptions? opts = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeClean(_handle, opts?.Handle ?? 0));
    }

    public string Commit(string msg, CommitOptions? opts = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeCommit(_handle, msg, opts?.Handle ?? 0, out var hash));
        return NativeMethods.ConsumeGoString(hash)!;
    }

    public string Move(string from, string to)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeMove(_handle, from, to, out var hash));
        return NativeMethods.ConsumeGoString(hash)!;
    }

    public void Pull(PullOptions? o = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreePull(_handle, o?.Handle ?? 0));
    }

    public void PullContext(PullOptions? o = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreePullContext(_handle, o?.Handle ?? 0));
    }

    public string Remove(string path)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeRemove(_handle, path, out var hash));
        return NativeMethods.ConsumeGoString(hash)!;
    }

    public void RemoveGlob(string pattern)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeRemoveGlob(_handle, pattern));
    }

    public void Reset(ResetOptions? opts = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeReset(_handle, opts?.Handle ?? 0));
    }

    public void Restore(RestoreOptions? o = null)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeRestore(_handle, o?.Handle ?? 0));
    }

    public Dictionary<string, FileStatus> Status()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeStatus(_handle, out var jsonPtr));
        var json = NativeMethods.ConsumeGoString(jsonPtr)!;
        return JsonSerializer.Deserialize<Dictionary<string, FileStatus>>(json) ?? new();
    }

    public Submodule GetSubmodule(string name)
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeSubmodule(_handle, name, out var resultHandle));
        return new Submodule(resultHandle);
    }

    public string[] Submodules()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
        NativeMethods.ThrowIfError(NativeMethods.GitWorktreeSubmodules(_handle, out var jsonPtr));
        var json = NativeMethods.ConsumeGoString(jsonPtr)!;
        return JsonSerializer.Deserialize<string[]>(json) ?? [];
    }

    public void Dispose()
    {
        if (_disposed) return;
        _disposed = true;
        NativeMethods.GitWorktreeFree(_handle);
        _handle = 0;
    }
}
